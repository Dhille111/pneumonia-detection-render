--extra-index-url https://download.pytorch.org/whl/cpu
Flask>=2.2
Pillow>=9.0
tqdm>=4.65.0
gunicorn>=20.1.0

# CPU-only PyTorch (use wheel available for Python 3.14 on Render)
torch==2.10.0+cpu
torchvision==0.25.0+cpu
